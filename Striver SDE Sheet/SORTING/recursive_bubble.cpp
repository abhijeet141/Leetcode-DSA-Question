#include<bits/stdc++.h>
using namespace std;

void bubble_sort(int *a){
    if(a[j])
}

int main(){
    int n;
    cin>>n;
    int *a = new int[n];
    for (int i = 0; i < n; i++)
    {
        cin>>a[i];
    }
    bubble_sort(a);
}